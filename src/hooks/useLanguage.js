export const useLanguage = (language, info) => {
    if(language === 'ENG'){
        return info.english
    }
    if (language === 'ESP'){
        return info.spanish
    }
    // if (language === 'FRA'){
    //     return info.french
    // }
    // if (language === 'ITA'){
    //     return info.italian
    // }
    } 